#!/usr/bin/env php
<?php

declare(strict_types=1);

// Included autoload.
include __DIR__.'/../autoload.php';

// Create Builder app instance.
$app = new Medz\GBT2260\ResourceBuilder\Application();

// Run the app handle.
$status = $app->run();

// Send exit code.
exit($status);
